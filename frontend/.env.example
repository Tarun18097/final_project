# Backend API URL — in dev the Vite proxy handles this automatically
# Only set this if your backend is on a different domain in production
VITE_API_URL=http://localhost:4000/api
VITE_SOCKET_URL=http://localhost:4000
